// Generated by CoffeeScript 1.3.3
(function() {
  var __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  this.TextUtils = (function() {

    function TextUtils() {
      this.fix_position_for = __bind(this.fix_position_for, this);

      this.split_to_elements = __bind(this.split_to_elements, this);

    }

    TextUtils.prototype.split_to_elements = function(copy, options) {
      var html, items, separator, tagName,
        _this = this;
      if (options == null) {
        options = {};
      }
      separator = options.separator || " ";
      tagName = options.tagName || "span";
      html = "";
      items = copy.split(separator);
      _.each(items, function(word, i) {
        var spacer;
        spacer = i < items.length - 1 ? " " : "";
        return html += "<" + tagName + ">" + word + spacer + "</" + tagName + ">";
      });
      return html;
    };

    TextUtils.prototype.fix_position_for = function(el) {
      var offset;
      log($(el).offset());
      offset = $(el).offset();
      log(offset);
      return $(el).position({
        position: "absolute",
        top: offset.top,
        left: offset.left
      });
    };

    return TextUtils;

  })();

}).call(this);
